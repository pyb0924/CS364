.486
;description
SSEG SEGMENT USE16
	STK DB 20 DUP(?)
SSEG ENDS
DSEG SEGMENT USE16
	OF_INFO DB "OF!",'$'
		
DSEG   ENDS

STRING_OUT MACRO INFO
	LEA    DX,INFO
	MOV    AH, 09H
	INT    21H
ENDM
CODE SEGMENT USE16
	     ASSUME CS:CODE,DS:DSEG,SS:SSEG
;description
MAIN PROC	
	MOV    EAX,3
	MOV EDI,1000000000
	MUL EDI
	JNO OVERFLOW
	STRING_OUT OF_INFO

	OVERFLOW:

	MOV AH,4CH
    INT 21H
	
MAIN ENDP
CODE   ENDS
       END  MAIN